# 功能:环境感知 (agent-cpp)

持续监听 Unity 写入的帧文件 + 系统音频,维护滚动上下文。

## 场景:帧文件扫描

- 只识别 `frame_*.jpg/png`
- 按文件名排序取最新
- 目录不存在时返回空而非崩溃

## 场景:视觉批次

- 已处理的帧不重复描述
- 并发描述有上限(不无限堆积)
- 描述结果记录时间/路径/描述/差异

## 场景:差异计算

- 首帧为 "initial view"
- 相同描述为 "unchanged"
- 变化时列出新增词(最多 8 个)

## 场景:滚动清理

- 超过时间窗的条目被归档为摘要(上限 50 条)
- 过期的帧文件被删除
- 摘要时间显示归档起点

## 场景:上下文输出

- 长期记忆/视觉时间线/听觉内容分节
- 无内容时返回占位文案
- minutes 参数控制回溯窗口
